apply plugin: 'com.android.application'
apply from: rootProject.file('lib_common.gradle')
//apply from: rootProject.file('app/sign/module_package.gradle')

android {
    defaultConfig {
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    api project(':lib:lib_wrapper')
}